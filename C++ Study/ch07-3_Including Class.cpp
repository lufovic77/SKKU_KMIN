#include <iostream>
/*
Inheritance violates the encapsulation.
Therefore, if two classes are in inclusion relation, we do not use an inheritance. 
Instead, we include the on class to the other. 
*/
int main(){
	
}
