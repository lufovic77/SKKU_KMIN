#include <stdio.h>
#include <algorithm>
#include <cstdio>
#include <vector>
#include <ctype.h>
#include <string.h>
using namespace std;
int main(){
	char str[10];
	puts(str);
	for(int i=0;i<10;i++)
	str[i]=i+65;
	printf("%s", str);
}
