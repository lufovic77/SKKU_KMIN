#include <stdio.h>
#include <stdlib.h>
int main() {
   int n;
   scanf("%d", &n);
   int k;
   scanf("%d", &k);
   int* value=(int*)malloc(sizeof(int)*n);
   
   int i;
   for(i=0;i<n;i++)
   scanf("%d", (value+i));
   int result=0;
   int 
   
   
   
   
   free(value);
}
